# Natural Language Log Parser

Deploy with `supabase functions deploy parse-log`.

The function returns a preview only. The mobile app must show the extracted records and the user must confirm before anything is written to the database.
