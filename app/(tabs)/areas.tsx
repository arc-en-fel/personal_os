import { useCallback, useState } from 'react';
import { Alert, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from 'react-native';
import { router, useFocusEffect } from 'expo-router';
import { useAuth } from '@/src/providers/AuthProvider';
import { supabase } from '@/src/lib/supabase';
import { colors, spacing } from '@/src/theme';
const builtInAreas = ['Fitness', 'Learning', 'Projects', 'Goals', 'Finance', 'Nutrition', 'Knowledge'];
type CustomArea = { id: string; name: string; description: string | null };
export default function AreasScreen() { const { session } = useAuth(); const [customAreas, setCustomAreas] = useState<CustomArea[]>([]); const [name, setName] = useState(''); const [description, setDescription] = useState(''); const [saving, setSaving] = useState(false); const [editingId, setEditingId] = useState<string | null>(null); const [editName, setEditName] = useState(''); const [editDescription, setEditDescription] = useState(''); const load = useCallback(async () => { if (!session) return; const { data } = await supabase.from('areas').select('id,name,description').order('created_at', { ascending: true }); setCustomAreas((data as CustomArea[] | null) ?? []); }, [session]); useFocusEffect(useCallback(() => { void load(); }, [load])); async function createArea() { if (!session || !name.trim()) return; setSaving(true); const { error } = await supabase.from('areas').insert({ user_id: session.user.id, name: name.trim(), description: description.trim() || null }); setSaving(false); if (error) Alert.alert('Could not create area', error.message); else { setName(''); setDescription(''); void load(); } } function startEdit(area: CustomArea) { setEditingId(area.id); setEditName(area.name); setEditDescription(area.description ?? ''); } async function updateArea() { if (!editingId || !editName.trim()) return; const { error } = await supabase.from('areas').update({ name: editName.trim(), description: editDescription.trim() || null }).eq('id', editingId); if (error) Alert.alert('Could not update area', error.message); else { setEditingId(null); void load(); } } function deleteArea(area: CustomArea) { Alert.alert('Delete area?', `${area.name} will be permanently removed.`, [{ text: 'Cancel', style: 'cancel' }, { text: 'Delete', style: 'destructive', onPress: async () => { const { error } = await supabase.from('areas').delete().eq('id', area.id); if (error) Alert.alert('Could not delete area', error.message); else void load(); } }]); } function openBuiltIn(area: string) { const routes: Record<string, '/fitness' | '/learning' | '/projects' | '/goals' | '/finance' | '/nutrition' | '/knowledge'> = { Fitness: '/fitness', Learning: '/learning', Projects: '/projects', Goals: '/goals', Finance: '/finance', Nutrition: '/nutrition', Knowledge: '/knowledge' }; const route = routes[area]; if (route) router.push(route); } return <ScrollView style={styles.screen} contentContainerStyle={styles.content}><Text style={styles.kicker}>THE BIG PICTURE</Text><Text style={styles.title}>Areas</Text><Text style={styles.subtitle}>The parts of life you are actively tending.</Text><View style={styles.grid}>{builtInAreas.map((area, index) => <Pressable key={area} onPress={() => openBuiltIn(area)} style={[styles.card, { backgroundColor: index % 2 ? '#F2E9D5' : '#DCE8D8' }]}><Text style={styles.icon}>{['O', '*', '[]', '#', 'o', '+', '~'][index]}</Text><Text style={styles.area}>{area}</Text><Text style={styles.meta}>Open area</Text></Pressable>)}</View><Text style={styles.sectionTitle}>Custom areas</Text>{customAreas.map(area => <View style={styles.customCard} key={area.id}>{editingId === area.id ? <><TextInput value={editName} onChangeText={setEditName} style={styles.input} /><TextInput value={editDescription} onChangeText={setEditDescription} style={styles.input} /><View style={styles.actions}><Pressable onPress={() => setEditingId(null)}><Text style={styles.cancel}>Cancel</Text></Pressable><Pressable onPress={() => void updateArea()}><Text style={styles.edit}>Save changes</Text></Pressable></View></> : <><Text style={styles.customTitle}>{area.name}</Text>{area.description && <Text style={styles.customDescription}>{area.description}</Text>}<View style={styles.actions}><Pressable onPress={() => startEdit(area)}><Text style={styles.edit}>Edit</Text></Pressable><Pressable onPress={() => deleteArea(area)}><Text style={styles.delete}>Delete</Text></Pressable></View></>}</View>)}<View style={styles.form}><Text style={styles.formTitle}>Create an area</Text><TextInput placeholder="e.g. Writing" placeholderTextColor={colors.muted} value={name} onChangeText={setName} style={styles.input} /><TextInput placeholder="What belongs here? (optional)" placeholderTextColor={colors.muted} value={description} onChangeText={setDescription} style={styles.input} /><Pressable disabled={saving || !name.trim()} onPress={() => void createArea()} style={[styles.button, (saving || !name.trim()) && styles.disabled]}><Text style={styles.buttonText}>{saving ? 'Creating...' : 'Create area'}</Text></Pressable></View></ScrollView>; }
const styles = StyleSheet.create({ screen: { flex: 1, backgroundColor: colors.paper }, content: { padding: spacing.lg, paddingTop: 64, paddingBottom: 100 }, kicker: { color: colors.sageDark, fontSize: 12, fontWeight: '800', letterSpacing: 2 }, title: { color: colors.ink, fontSize: 34, fontWeight: '800', marginTop: 6 }, subtitle: { color: colors.muted, fontSize: 16, lineHeight: 23, marginBottom: spacing.xl, marginTop: spacing.sm }, grid: { flexDirection: 'row', flexWrap: 'wrap', gap: spacing.sm }, card: { borderRadius: 16, padding: spacing.md, width: '48%' }, icon: { color: colors.ink, fontSize: 23 }, area: { color: colors.ink, fontSize: 17, fontWeight: '800', marginTop: spacing.lg }, meta: { color: colors.muted, fontSize: 12, marginTop: 5 }, sectionTitle: { color: colors.ink, fontSize: 20, fontWeight: '800', marginBottom: spacing.sm, marginTop: spacing.xl }, customCard: { backgroundColor: colors.card, borderRadius: 14, marginBottom: spacing.sm, padding: spacing.md }, customTitle: { color: colors.ink, fontSize: 16, fontWeight: '800' }, customDescription: { color: colors.muted, marginTop: 5 }, form: { backgroundColor: '#F2E9D5', borderRadius: 16, marginTop: spacing.md, padding: spacing.md }, formTitle: { color: colors.ink, fontSize: 18, fontWeight: '800' }, input: { backgroundColor: colors.card, borderColor: colors.line, borderRadius: 11, borderWidth: 1, color: colors.ink, fontSize: 16, marginTop: spacing.sm, padding: spacing.md }, button: { alignItems: 'center', backgroundColor: colors.ink, borderRadius: 11, marginTop: spacing.sm, padding: spacing.md }, disabled: { opacity: .45 }, buttonText: { color: colors.card, fontWeight: '800' }, actions: { flexDirection: 'row', gap: spacing.lg, marginTop: spacing.sm }, edit: { color: colors.sageDark, fontSize: 13, fontWeight: '800' }, delete: { color: colors.coral, fontSize: 13, fontWeight: '800' }, cancel: { color: colors.muted, fontSize: 13, fontWeight: '800' } });
